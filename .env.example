# --- メール送信（Microsoft 365 / Graph API: client credentials flow）---
# 実値は .env.local（gitignore済み）に設定してください。
# 本番（Vercel等）はホスティングの環境変数に設定します。
MS_TENANT_ID=
MS_CLIENT_ID=
MS_CLIENT_SECRET=
MAIL_FROM_ADDRESS=noreply@ktgs.llc
MAIL_FROM_NAME=ROOTA
MAIL_TO_ADDRESS=office360@ktgs.llc
