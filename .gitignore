/node_modules
/.next/
/out/
/build
.DS_Store
*.pem
npm-debug.log*
.env*
next-env.d.ts
*.tsbuildinfo

# hero source originals (kept locally for re-cropping)
/public/*.orig.jpg

# allow committing the env template (but never real .env files)
!.env.example

# Local editorial / source artifacts — not part of the deployed site
/*.md
/*.pptx
/AI*.png
/.claude/

# Local HTML mockups
/*.html
